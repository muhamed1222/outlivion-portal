# Outlivion Web Portal

Веб-портал для управления VPN подпиской Outlivion.

## 🚀 Быстрый старт

### Требования
- Node.js 20+
- pnpm (рекомендуется) или npm

### Установка

```bash
# Установить зависимости
pnpm install

# Настроить .env
cp env.example .env.local
# Отредактируйте .env.local со своими настройками
```

### Разработка

```bash
# Запустить в dev режиме
pnpm dev

# Откройте http://localhost:3000
```

### Production

```bash
# Собрать проект
pnpm build

# Запустить production сервер
pnpm start
```

## 📦 Основные команды

| Команда | Описание |
|---------|----------|
| `pnpm dev` | Запуск в dev режиме |
| `pnpm build` | Сборка для production |
| `pnpm start` | Запуск production сервера |
| `pnpm lint` | ESLint проверка кода |

## 🔧 Переменные окружения

Создайте файл `.env.local`:

```env
# API URL
NEXT_PUBLIC_API_URL=http://localhost:3001

# Telegram Bot
NEXT_PUBLIC_TELEGRAM_BOT_NAME=your_bot_name
```

## 📱 Страницы

### Публичные
- `/` - Главная страница
- `/login` - Вход через Telegram
- `/terms` - Условия использования
- `/faq` - FAQ

### Защищенные (требуется авторизация)
- `/dashboard` - Личный кабинет
- `/billing` - Оплата подписки
- `/billing/success` - Успешная оплата
- `/profile` - Профиль пользователя
- `/config/[serverId]` - VLESS конфигурация
- `/servers` - Список серверов
- `/transactions` - История платежей
- `/promo` - Активация промокодов

## 🎨 UI Компоненты

Используются:
- **TailwindCSS** - Стилизация
- **Radix UI** - Headless компоненты
- **shadcn/ui** - UI библиотека
- **Motion** - Анимации
- **kokonutui** - Дополнительные компоненты

## 🔐 Безопасность

- ✅ Next.js middleware для защиты роутов
- ✅ Автоматическое обновление JWT токенов
- ✅ Безопасное хранение токенов
- ✅ CSRF защита

## 🛠 Технологии

- **Next.js 14** - React framework (App Router)
- **React 18** - UI библиотека
- **TypeScript** - Язык программирования
- **TailwindCSS** - CSS framework
- **Axios** - HTTP клиент

## 📊 Структура проекта

```
src/
├── app/               # Next.js App Router pages
│   ├── billing/      # Страницы оплаты
│   ├── config/       # Конфигурация серверов
│   ├── dashboard/    # Личный кабинет
│   ├── login/        # Авторизация
│   ├── profile/      # Профиль
│   ├── promo/        # Промокоды
│   └── transactions/ # История платежей
├── components/        # React компоненты
│   ├── layout/       # Layout компоненты
│   ├── ui/           # UI компоненты
│   └── kokonutui/    # Kastom компоненты
├── lib/              # Утилиты и API клиент
└── styles/           # Глобальные стили
```

## 📄 Лицензия

ISC

---

**Часть Outlivion VPN Platform**
